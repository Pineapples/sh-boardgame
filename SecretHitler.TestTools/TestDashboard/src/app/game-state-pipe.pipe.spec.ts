import { GameStatePipePipe } from './game-state-pipe.pipe';

describe('GameStatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new GameStatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
